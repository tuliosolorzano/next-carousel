'use client';

import styled, { keyframes, css } from 'styled-components';

const CarouselContainer = styled.div`
  position: relative;
  width: 100%;
  overflow: hidden;
  margin-top: 15.8rem;
  margin-bottom: 15.8rem;
`;

const Carousel = () => {
  return <CarouselContainer>Carousel</CarouselContainer>;
};

export default Carousel;
